networks:
  cats:

services:
  nginx:
    image: nginx:latest
    ports:
      - "443:443"
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
      - '/etc/letsencrypt:/etc/letsencrypt:ro'
    depends_on:
      - frontend
    networks:
      - cats

  frontend:
    build:
      context: ./app
    networks:
      - cats